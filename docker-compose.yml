version: '3.8'
services: 
  webserver:
    image: nginx:alpine
    container_name: portfolio_web_server
    restart: always 
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html
